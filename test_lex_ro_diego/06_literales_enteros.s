// Literales enteros validos
start {
    Int x;
    Int y;
    Int z;

    // Enteros basicos
    x = 0;
    y = 123;
    z = 999999;

    // Enteros en diferentes contextos
    x = 42 + 58;
    y = 1000 - 500;
    z = 12 * 3;

    // Operadores ++ y --
    ++x;
    --y;
}
